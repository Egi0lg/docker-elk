input {
    redis {
        data_type => "channel"
        key => "logstash"
        host => "redis"
        port => 6379
        codec => "json"
    }
}

## Add your filters here

output {
    elasticsearch {
        hosts => "elasticsearch:9200"
    }
}